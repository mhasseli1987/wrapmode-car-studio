# START HERE — Wrapmode Car Studio

> راهنمای شروع سریع برای سشن جدید. این فایل را اول بخوان.

## پروژه چیست

یک **سایت تک‌صفحه‌ای سینمایی** برای استودیو رپ خودرو **WRAPMODE**
(`wrapmode.ir`): کاربر اسکرول می‌کند و یک **ریل ۲۴۱ فریمی** روی `<canvas>`
فریم‌به‌فریم اسکراب می‌شود — ۵ فصل، هر فصل یک طرح رپ فول‌بادی.
انتهای صفحه: کالکشن کارت‌ها + سبد خرید محلی + ثبت سفارش در واتساپ.

## ⚠️ مسیر واقعی پروژه

```
✅ site/          ← همه‌چیز اینجاست. فقط این را ویرایش کن.
❌ app/ worker/ db/ examples/ package.json vite.config.ts   ← باقی‌ماندهٔ استارتر، بی‌استفاده
```

استقرار: **Netlify** — `netlify.toml`: `publish = "site"`, `command = ""`
(هیچ بیلدی ندارد؛ فایل‌ها همان‌طور که هستند سرو می‌شوند.)

## نقشهٔ فایل‌های مهم

| فایل | چیست |
|---|---|
| `site/index.html` | تنها صفحه. هدر پیل، هیرو، `#scrub-stage`، کالکشن، فوتر، سبد، مودال تماس |
| `site/js/app.js` | موتور کامل: لود فریم‌ها، حلقهٔ رندر، تایم‌لاین، کپشن‌ها، سبد، i18n |
| `site/css/style.css` | تمام استایل‌ها (CSS خام، بدون فریم‌ورک) |
| `site/frames4/` | ۲۴۱ فریم webp — منبع اسکراب (استفاده می‌شود) |
| `site/img/` | تصاویر کارت‌ها + لوگو + امضای ENZO |
| `SESSION SAVED/*.md` | تاریخچهٔ سشن‌ها — برای context |

## پارامترهای کلیدی (`app.js`)

```js
FRAME_DIR = "frames4/"     FRAME_COUNT = 241    GRID_HOLD = 7   // فریم هیرو
CHAPTERS = [{14,48}, {50,96}, {100,144}, {148,188}, {192,240}]   // ۵ فصل
CH_WEIGHTS = [0.16, 0.21, 0.21, 0.19, 0.23]     // سهم اسکرول هر فصل
INTRO_END = 0.05    CH_FADE = 5                 // هیرو / فید بین فصل
WA_NUMBER = "989196828013"                      // واتساپ فروشگاه
lang = "en"                                     // سایت فقط انگلیسی
```

## قواعد طلایی (نقض = خشک شدن اسکرول)

۱. **فقط یک حلقه.** `gsap.ticker` با fallback به rAF. هرگز `setInterval` موازی.
۲. **میرایی مستقل از فریمریت:** `alpha = 1 - Math.exp(-dt * k)`، هرگز `d * 0.38`.
۳. **هیچ خواندن layout داخل حلقه.** `scrollMax` و `scrubLen` کش می‌شوند.
۴. **نوشتن DOM فقط وقتی مقدار عوض شد:** `if (next !== last) last = next;`
۵. **بافر canvas یک‌بار:** `canvasW/canvasH` کش + early-return.
۶. **Lenis با `lerp`** نه `duration`.
۷. **`x = scrollY / scrubLen`** — صورت و مخرج یکی باشند.
۸. هر چیز بیرونی (Lenis/GSAP از CDN) را با `typeof` گارد کن.

## کارهای باز

- [ ] کامیت + پوش (سشن ۰۷ و ۰۸ هر دو انباشته)
- [ ] تأیید بصری روانی اسکرول توسط کاربر
- [ ] حذف `site/frames3/` (~۴۷MB)
- [ ] تبدیل `frames4/` به AVIF
- [ ] حذف کد مردهٔ استارتر (`app/`, `worker/`, `db/`, `examples/`, `tmp-new/`)
- [ ] تست خودکار دائمی (فعلاً اسکریپت موقت بوده)

## اجرای محلی

```bash
cd site && python -m http.server 8899
# سپس: http://localhost:8899/index.html
# ?nomotion  → تست بدون اسکرول نرم (Lenis خاموش)
```

## نکتهٔ زبان پاسخ‌دهی

کاربر **فارسی** می‌خواهد، **راست‌چین**، **کوتاه و نتیجه‌گرا** (کم‌مصرف).
