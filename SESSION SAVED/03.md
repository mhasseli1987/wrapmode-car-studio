# SESSION 03 — 2026-08-27

## کار این سشن
1. **فریم‌های جدید ماشین ۱ از خود کاربر** — زیپ `Downloads/clean_mobile_frames_1080x1920.zip` (۵۱ فریم JPG عمودی ۱۰۸۰×۱۹۲۰ از ezgif، نه ۵۰):
   - تبدیل به webp q92 در `site/frames6/car1/f_0001..0051.webp` (فقط ۴.۹MB)
   - موتور `app.js` شد per-car: `CARS[n].count` (car1=51، بقیه=151) — timelineAt/nearestLoaded/prefetchAround/preload همه count متغیر
   - قیمت‌ها: 48.5/52/45/60 میلیون تومان
2. **فول‌اسکرین از هر ۴ طرف (درخواست صریح کاربر «از همه طرف فیکس»)**
   - car1: `fit:"cover"` — فریم ۹:۱۶ native کل صفحه را می‌پوشاند (کمی برش بالا/پایین)
   - car2-4 (فریم مربع 1520): **پس‌زمینه‌ی محو از خود فریم** (`bgBlur`: drawImage در بوم 20×36 → بزرگ‌شده = گرادیان نرم همرنگ، ارزان) + عکس اصلی contain با **ماسک آلفای لبه‌ی نرم** (`maskCanvas` گرادیان افقی+عمودی destination-in روی `edgeCanvas` هر فریم، محو ۱۰٪ هر طرف) — بدون لبه‌ی سخت
3. رفع اشکال‌های این سشن:
   - سرور 8123 وسط کار مرد → دوباره `python -m http.server 8123` از `site/` (بک‌گراند exec_42158a7f)
   - `tab.goto` با هش فقط anchor-jump می‌کند و **reload نمی‌کند** — برای اعمال js جدید حتماً `tab.reload()`
   - اسکرین‌شات IAB بعد از مرگ سرور «capture failed for guest» می‌دهد → reload بعد از بالا آمدن سرور
   - `analyze_image` MCP این سشن کار کرد (URL از Read CDN) — برخلاف سشن قبل
   - Read تصاویر پشت‌سرهم URL تکراری برمی‌گرداند → diff پیکسلی برای اطمینان از جدید بودن

## وضعیت فنی فعلی
- `site/js/app.js` v=9: بدون ScrollTrigger، progress = scrollY/maxScroll، gsap.ticker + fallback setInterval(33ms)، Lenis با kill-switch، کراس‌فید XF_LOCAL=0.068، FRAME_PART=0.70
- draw: `drawImageCover(car,i,alpha,sx,sy)` → cover برای car1؛ برای contain: bgBlur پرکننده + `drawContainSoft` با ماسک
- `site/index.html`: preloadهای frames6/car1، `js/app.js?v=9`
- `netlify.toml`: کش immutable برای frames5 و frames6
- کامیت‌های سشن: `50f1ee9` (فریم‌های کاربر+per-car) → `7cf668f` (cover فول‌بلید) → `bc53316` (پرکننده+لبه نرم v9)
- tmp-user-frames/ و tmp-*.png در .gitignore

## باز leftover / پیشنهاد بعدی
- اگر کاربر خواست car2-4 هم native فول‌اسکرین: ساخت فریم ۱۰۸۰×۱۹۲۰ از `source-videos/car2-4.mp4` (الان blur-fill دارند)
- گام‌های چرخش car1 = ۵۱ (پرش‌دارتر از ۱۵۱) — اگر گله کرد: interpolation میکس پیکسلی بین دو فریم در render
- دیپلوی Netlify هنوز انجام نشده (netlify.toml آماده، فقط کانکت ریپو)

## قوانین کاربر (ثابت)
- جواب کوتاه، فارسی، راست‌چین، فقط نتیجه
- «این چت ذخیره کن» → SESSION SAVED\NN.md (بعدی: 04)
- هر تصویر تولیدی → پوشه /reference/ + پوش
