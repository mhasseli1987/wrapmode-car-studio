# SESSION SAVED 07 — ۲۰۲۶-۰۹-۰۹ (بعدازظهر)

ادامه از session 06 (کامیت `db5b3ec`). این سشن یک زنجیره ویرایش UI روی `site/` بود، همه با تست زنده در IAB (پورت 8123). **همه‌ی تغییرات هنوز کامیت نشده‌اند** — commit بعد از تأیید کاربر.

نسخه‌ها: `css/style.css?v=27` — `js/app.js?v=35`.

---

## ۱) حذف‌ها

- اسپن کپی‌رایت فوتر «© 2026 WRAPMODE — ALL RIGHTS RESERVED» حذف
- دکمه‌ی شناور واتساپ `#wa-float` کامل حذف (HTML + CSS + JS)
- تیترهای فارسی کپشن‌ها: ۵ عنصر `h2.cap-title` از index.html + کلیدهای `cap1..5.title` از هر دو دیکشنری i18n + همه‌ی رول‌های CSS `.cap-title` حذف

## ۲) لوگوهای واقعی سوشال + شماره‌ی واقعی

- آیکون‌های واتساپ/اینستاگرام فوتر → SVG برندهای واقعی (کپی از `wrapmode-local/wrapmode-site/src/components/Footer.tsx`): واتساپ `#25D366` → `wa.me/989196828013`؛ اینستاگرام گرادیان → `instagram.com/car.vinyl?igsh=YnowNXN0MXdpNm53`
- **`WA_NUMBER = "989196828013"`** در app.js اصلاح شد (شماره واقعی سایت) — قبلاً جاوااسکریپت href فیک روی HTML حاکم می‌شد

## ۳) هدر جدید = پیل سایت اصلی (مهم‌ترین تغییر)

- هدر متنی قدیمی حذف؛ جای آن **پیل شناور** عین wrapmode.ir: `logo-transparent.webp` (کپی از سورس لوکال → `site/img/`) → https://wrapmode.ir | ۸ لینک واقعی ناوبری محصولات/تماس/درباره | سبد کهربایی (درور محلی) | دو CTA کهربایی «Direct Contact» (مودال تماس محلی) + «Track Order» (wrapmode.ir/order/track/) | همبرگر <۱۰۲۴px با درور
- ترجمه ۱:۱ کلاس‌های Tailwind → CSS خام: breakpoint نوار ناوبری ۱۰۲۴px، پیل بلند/CTA/بوردر سفید ≥۷۶۸px، بوردر کهربایی موبایل
- دکمه‌ی تم سایت اصلی عمداً گذاشته نشد (سینماییِ همیشه‌تیره)

## ۴) انگلیسی‌سازی کامل

- دکمه‌ی `#lang-toggle` حذف شد — سایت **فقط انگلیسی**: `let lang = "en"` ثابت، initLang حذف، `<html lang="en" dir="ltr">`، title/meta انگلیسی، اعداد/قیمت‌ها فرمت لاتین

## ۵) رِورس هدر

- `flex-direction: row-reverse` روی `.header-pill` و `.head-actions` → **لوگو راست، منو/سبد چپ** (فقط CSS؛ لینک‌ها دست‌نخورده)

## ۶) هیرو

- `Scroll` hint: ۱.۱۵rem/۸۰۰ + SVG ۲۶px
- کیکر زرد `CAR WRAP STUDIO`: `clamp(1rem, 2.6vw, 1.3rem)`، وزن **۷۰۰** (+ Cormorant 700 به لینک فونت)، text-shadow
- **حذف دایره‌ی سفید بوکه از روی کیکر:** دایره در فریم‌های ۷/۸/۹ (frames4) از پشت متن دریفت می‌کرد → inpaint Telea با ماسک دیسکی r=۱۱۰ (مرز ماسک باید داخل پس‌زمینه‌ی تمیز بیفتد، وگرنه شبح خاکستری می‌ماند — پاس اول با r≈۷۰ همین شکلی شد)؛ **بکاپ سه فریم اصلی: `C:\tmp\frames_backup\`**

## ۷) کپشن‌ها (اسم کارت‌ها)

- فقط انگلیسی؛ `.cap-kicker` ارتقا یافت به تیتر: `clamp(1.45rem, 5.5vw, 2.6rem)` / ۷۰۰ / طلایی / text-shadow + هیرلاین‌های طلایی دو طرف

## ۸) سربرگ کالکشن — گلسی مات

- `.col-head` پنل frosted شد: `backdrop-filter: blur(22px) saturate(140%)` + bg `rgba(10,8,14,.32)` + رادیوس ۲۲ + بوردر/سایه
- **نکته:** پس‌زمینه‌ی `.collection` از گرادیان کاملاً مات → نیمه‌شفاف (`rgba(5,4,8,.55)→rgba(12,9,19,.78)`) شد تا بلور واقعاً پشتش دیده شود (بوم ثابت ساکورا)

## ۹) فوتر

- وردمارک `WRAPMODE` بزرگ وسط: Cormorant `clamp(3rem, 10.5vw, 7.2rem)`، opacity ۰.۷۸، nowrap — واترمارک سینمایی
- `.foot-meta` (لوگوی ENZO + واتساپ/اینستاگرام) → `position:absolute; bottom:26px; left:…` گوشه‌ی پایین-چپ؛ ENZO قد ۳۸px (موبایل ۳۰)
- خط طلایی وسط بلندتر: `clamp(110px, 18vw, 180px)`

---

## گیت

تغییرات uncommitted: `M site/{index.html, css/style.css, js/app.js, frames4/f_0007..9.webp}` + `?? site/img/{logo-transparent.webp, made-by-enzo.png}`. پوش = ~۹۰KB/s + ناپایدار ⇒ retry. Netlify هنوز انجام نشده. `site/frames3/` (۴۷MB) قابل حذف.

## درس‌های computer-use/IAB

- AXPress روی دکمه‌های صفحه گاهی هندلر JS را شلیک نمی‌کند ⇒ `open_application(activate=true)` سپس کلیک مختصاتی `strategy="event"` (پیکسل رستر ۱۲۸۰×۷۲۰)
- بعد از رفرش، `frame_id` اسکرول‌ها منقضی می‌شود ⇒ screenshot تازه
- صفحه‌ی IAB جای اسکرول را حفظ می‌کند؛ برای رفتن به فوتر: کلید `End` (Lenis target-based است ولی scroll listener معمولی هم جواب داد)
- `analyze_image` (Z.ai) به `127.0.0.1` دسترسی ندارد — فقط URL عمومی کار می‌کند؛ برای کراپ‌های ریز از آمار پیکسل/موزاییک cv2 + Read استفاده شد
