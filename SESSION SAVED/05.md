# SESSION SAVED 05 — ۲۰۲۶-۰۹-۰۸

ادامه از v21. این سشن: نصب اسکیل‌های انیمیشن + پاس «حس اسکرول» (v22). آخرین کامیت: `741e029` (پوش شده، یک‌باره).

---

## ۱) نصب اسکیل‌ها (درخواست کاربر: «هر اسکیلی نیازه پیدا کن و اجرا کن»)

- جستجو با `npx skills find` → انتخاب `scroll-animations` (۹۶۶ نصب) + `gsap-greensock` از مخزن `dylantarre/animation-principles`
- **گوتچا:** `npx skills add` ناقص کار کرد (پوشه خالی `animation-vocabulary` ساخت) → حل: دانلود مستقیم دو SKILL.md از raw.githubusercontent به `~/.agents/skills/scroll-animations/` و `~/.agents/skills/gsap-greensock/`
- محتوای هر دو سالم و امن (۱۲ اصل انیمیشن دیزنی برای اسکرول + الگوهای GSAP)

## ۲) v22 — پاس حس اسکرول (css `?v=14`، js `?v=22`)

| # | بهبود | جزئیات |
|---|---|---|
| ۱ | **بلند بین‌فریمی** | `timelineAt` حالا idx **اعشاری** برمی‌گرداند؛ `drawFrame` دو فریم floor/ceil را با آلفای کسری روی هم می‌کشد → سورس ۲۴fps به حرکت پیوسته ۶۰fps رسید (بزرگ‌ترین بهبود) |
| ۲ | **زوم ریز سرعت‌محور** | `zoom = 1 + min(|targetQ−smoothQ|·1.1, 0.035)` — اسکرول تند = کشش خیلی ملایم، توقف = برگشت آرام |
| ۳ | **نوار پیشرفت طلایی** | `#progress` ثابت بالای صفحه، `scaleX` روی کل سند؛ هر تیک آپدیت می‌شود (نه فقط وقتی x عوض می‌شود — وگرنه در کالکشن یخ می‌زد)؛ origin راست در fa / چپ در en |
| ۴ | **دریفت کپشن‌ها** | `--drift = (0.5−p)·26px` — تایتل‌کارت‌ها مثل فیلم شناور؛ CSS: `translateY(calc(-50% + var(--drift)))` |
| ۵ | **ریویل پله‌ای کالکشن** | IO (threshold .15، rootMargin −8%) + gsap fade-up ‏0.65s با تأخیر پله‌ای 0.09؛ فقط بار اول (`initReveals` از انتهای `renderCards`، IO کشته و از نو) |

- گارد `prefersReducedMotion` برای ریویل — درست کار می‌کند

## ۳) تست (IAB، ۱۲۸۰×۸۰۰)

- هیرو ✓ / کپشن «آنیمه شب» + دریفت −12.3px در x=0.42 ✓ / progress=scaleX(1) در انتهای سند ✓ / کارت‌ها opacity 1 ✓
- **نکته تست:** `?nomotion=1` عمداً ریویل را هم خاموش می‌کند (چون `prefersReducedMotion` را true می‌کند) و IAB خودش reduce=true گزارش می‌دهد → ریویل فقط در مرورگر واقعی دیده می‌شود؛ با پروب درون‌صفحه تست کن، نه اسکرین‌شات
- **گوتچا:** هر فراخوانی browser-use یک کرنل تازه است — در هر سلول bootstrap + `tabs.get(id)` از نو

---

## باقی‌مانده
- قیمت‌ها/اسم‌ها همچنان placeholder — منتظر اعداد واقعی کاربر
- **Netlify هنوز وصل نشده**
- سقف کیفیت فریم = سورس ۱۱۵۲×۷۶۸ کاربر (خروجی با رزولوشن بالاتر از ابزار AI خودش → تیزی واقعی)
- طرح گرافیتی فقط کارت دارد، در فیلم کلیپ نیست
- سشن بعدی: `SESSION SAVED\06.md`
