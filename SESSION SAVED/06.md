# SESSION SAVED 06 — ۲۰۲۶-۰۹-۰۹

ادامه از v22. این سشن: کلیپ جدید `006.mp4` → کامل v23 (حذف واترمارک + فریم‌های روان + ست کامل سایت). کامیت اصلی: `8377842` (پوش شده، ~۱۸ دقیقه، یک‌باره) + کامیت جاری (پاک‌سازی گیت + ذخیره سشن).

---

## ۱) کلیپ جدید

- `reference/006.mp4`: ۱۰.۰۴ ثانیه، **۷۲۰×۱۲۸۰ عمودی**، ۲۴fps، ۲۴۱ فریم — دقیقاً همان مشخصات take قبلی (دروپ-این جایگزین)
- واترمارک متنی «Happy Horse 1.1» پایین-راست (باکس 430,1196 ابعاد 282×54) — از همین فریم سیاه اول هم حاضر بود → delogo روی همه‌ی فریم‌ها اعمال شد
- `reference/*.mp4` در `.gitignore` است (خط ۵۷) — سورس لوکال می‌ماند

## ۲) پایپ‌لاین فریم‌ها (یک پاس ffmpeg)

```
delogo=x=430:y=1196:w=282:h=54, scale=1440:2560:flags=lanczos, cas=0.5   →   webp q95
```

- خروجی: `site/frames4/` — ۲۴۱ فریم، ۸۷MB — تأیید بصری: واترمارک صفر

## ۳) نقشه‌ی فصل‌ها (contact sheet + ۴ نوار ترنزیشن با گام ۲)

| فریم | فصل |
|---|---|
| 0-12 | بوکه‌ی تاریک (هیرو — `GRID_HOLD=7`) |
| 14-48 | سامورایی موج (BMW قرمز/فیروزه، موج + سامورایی + کانجی) |
| 50-96 | آنیمه مجنتا (کوپه نقره‌ای، ترنزیشن ~f49) |
| 100-144 | هپی هورس ۱.۱ (سوپرای سفید، کمدی‌بوک جوکر، ترنزیشن ~f98) |
| 148-188 | گرافیتی نئون (بدنه قرمز، نور سبز، ترنزیشن ~f146) |
| 192-240 | ساکورا شب (کوپه مشکی، ابرهای صورتی، ترنزیشن ~f190) |

- `CH_WEIGHTS = [0.16, 0.21, 0.21, 0.19, 0.23]` (جمع = ۱)

## ۴) ست کامل سایت (js `?v=23`)

- `FRAME_DIR = frames4` — ساختار v22 (بلند بین‌فریمی/زوم سرعت/نوار پیشرفت/دریفت کپشن/ریویل پله‌ای) دست‌نخورده
- پنجمین `.cap` به index.html اضافه شد؛ `cap1..cap5` دوزبانه
- **DESIGNS جدید به ترتیب فیلم:** SAMURAI WAVE ۴۵م / MAGENTA ANIME ۴۲م / HAPPY HORSE ۳۸م / NEON GRAFFITI ۳۲م / SAKURA NIGHT ۲۸م (همان ۵ اسلات قیمت قبلی — منتظر تأیید اعداد واقعی)
- استیل کارت‌ها: `img/design-1..5.webp` = برش ۱۴۴۰×۹۰۰ از f45/f73/f121/f169/f221 با آفست y=620/880/750/900/790 — **گوتچا: سامورایی از f33 کلوزآپ خیلی تنگ بود؛ f45 درست**؛ design-8.webp قدیمی حذف شد
- preloads → کلیدفریم‌های frames4 (7/15/51/101/149/193)؛ `netlify.toml` → هدر immutable فریم‌های frames4

## ۵) نکته فنی مهم — فریم عمودی روی مانیتور افقی

- در `drawOne` شرط پرتره/منظره بر اساس **نسبت بوم** است نه فریم → فریم ۹:۱۶ روی دسکتاپ = **کاور فول‌بلید با برش باند مرکزی** (~۳۸٪ ارتفاع فریم؛ ماشین دقیقاً وسط فریم‌هاست، پس جواب می‌دهد و سینمایی است)
- شاخه‌ی زیرلایه‌ی بلور فقط برای ویوپورت عمودی (موبایل) اجرا می‌شود

## ۶) تست زنده

- `npx http-server -p 8123` از `site/` — هیروی بوکه + تایپوگرافی ✓ / فصل‌ها فول‌بلید و تیز ✓ / کپشن cap5 زنده («ساکورا — شبِ گل‌ریزان» + DESIGN 05 — SAKURA NIGHT) ✓ / ۵ کارت جدید با قیمت‌ها ✓ / فوتر و آمار ✓
- **گوتچا computer-use ویندوز:** کلید به اپ غیرفرانت = `frontmost_pid_mismatch` → اول `open_application(activate=true)` بعد key با `strategy=event`؛ DevTools با F12 تاگل می‌شود (ناخواسته باز شد)

## ۷) گیت

- `8377842` = v23 (frames4 + تایم‌لاین + کالکشن + reference کپی‌ها) — پوش موفق
- کامیت جاری: حذف ۳۱ تصویر reference قدیمی (پاک‌سازی خود کاربر؛ worktree-delete های معوق) + `.gitignore` += `/.freebuff/` + ذخیره سشن ۰۶

---

## باقی‌مانده

- تأیید/اصلاح قیمت‌های ۵ طرح (الان placeholder روی اسلات‌های قدیمی)
- **Netlify هنوز وصل نشده**
- کاندید پاک‌سازی: `site/frames3/` (~۴۷MB، فعلاً برای rollback مانده)
- سشن بعدی: `SESSION SAVED\07.md`
